name: Release checks

on:
  push:
  pull_request:

jobs:
  locked-v1-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Check Wide Docks release invariants
        run: python scripts/check_release.py
